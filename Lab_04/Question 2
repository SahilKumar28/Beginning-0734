#include<stdio.h>
int main(){
	int no;
	printf("Enter a number:");
	scanf("%d",&no);
    if((no>0)&&(no%2==0)&&(no%10==0)){
    	printf("The number is fine");
	}
	else{
		printf("Try again please");
	}
}
