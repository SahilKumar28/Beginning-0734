#include<stdio.h>
int main(){
	//I will use do-while loop.
	int no,sum=0;
	printf("Enter a no:");
	scanf("%d",&no);
	if(no!=0){
		do{
			sum=sum+no;
			printf("%d\n",sum);
            printf("Enter a no:");
	        scanf("%d",&no);			
		}while(no!=0);
	}
}
